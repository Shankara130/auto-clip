services:
  neo4j:
    image: neo4j:5
    ports:
      - "7474:7474" # HTTP - Neo4j Browser
      - "7687:7687" # Bolt - protokol koneksi untuk program
    environment:
      NEO4J_AUTH: neo4j/autoclipdev
    volumes:
      - neo4j_data:/data
volumes:
  neo4j_data:
